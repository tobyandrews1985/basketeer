# build output (published via prepublishOnly, not committed)
dist/
*.tsbuildinfo

# deps
node_modules/

# os / editor
.DS_Store
.claude/

# local working notes / scratch (agent handoffs, captures) — not for the public repo
state/
marketing/

# local secrets & harvested sessions — never commit
.env
.env.*
*.session
.basketeer/
